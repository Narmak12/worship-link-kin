# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/

# Android
android/local.properties
android/key.properties
android/app/*.keystore
android/app/*.jks

# Firebase : un google-services.json PLACEHOLDER est inclus pour que Gradle
# ne bloque pas. Remplace-le par ton vrai fichier (Firebase Console) dès que
# possible — ce n'est pas un secret tant qu'il reste un placeholder.

# macOS/IDE
.DS_Store
